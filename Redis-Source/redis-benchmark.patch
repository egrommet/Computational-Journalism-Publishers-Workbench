--- redis-benchmark.orig	2013-01-05 18:47:06.190210143 -0800
+++ redis-benchmark.c	2013-01-05 19:05:34.144097152 -0800
@@ -335,7 +335,8 @@
         }
         printf("%.2f requests per second\n\n", reqpersec);
     } else if (config.csv) {
-        printf("\"%s\",\"%.2f\"\n", config.title, reqpersec);
+        printf("\"%s\",\"%d\",\"%lld\",\"%.3f\"\n", config.title, 
+            config.requests_finished, config.totlatency, reqpersec);
     } else {
         printf("%s: %.2f requests per second\n", config.title, reqpersec);
     }
@@ -630,7 +631,7 @@
         if (test_is_selected("lrange") ||
             test_is_selected("lrange_100") ||
             test_is_selected("lrange_300") ||
-            test_is_selected("lrange_500") ||
+            test_is_selected("lrange_450") ||
             test_is_selected("lrange_600"))
         {
             len = redisFormatCommand(&cmd,"LPUSH mylist %s",data);
@@ -650,9 +651,9 @@
             free(cmd);
         }
 
-        if (test_is_selected("lrange") || test_is_selected("lrange_500")) {
+        if (test_is_selected("lrange") || test_is_selected("lrange_450")) {
             len = redisFormatCommand(&cmd,"LRANGE mylist 0 449");
-            benchmark("LRANGE_500 (first 450 elements)",cmd,len);
+            benchmark("LRANGE_450 (first 450 elements)",cmd,len);
             free(cmd);
         }
 
